services:
  flask_app:
    build: .
    env_file:
      - .env
    restart: always

  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "8081:80"
    env_file:
      - .env
    depends_on:
      - flask_app
    restart: always

